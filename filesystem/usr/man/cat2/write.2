

write (2)                    3/19/82                    write (2)


     NNAAMMEE
          wwrriittee - write to a file
          
          
     CC
          write(descriptor, buf, nbytes)
          char *buf;
          
          
     FFUUNNCCTTIIOONN
          This  call  writes  nbytes from the indicated buffer to
          the given open  file.  The  number  of  bytes  actually
          written  is returned. Unlike the rreeaadd call, this number
          should be the same as requested; otherwise, an error is
          indicated.
          
          
     RREETTUURRNNSS
          A -1 indicates an error (bad descriptor or physical  IO
          error). Otherwise, the value nbytes is returned.
          
          
     AASSSSEEMMBBLLEERR
          ((wwrriittee == 44))
          ((ddeessccrriippttoorr iinn hhll))
          ssyyss;; wwrriittee;; bbuuff;; nnbbyytteess
          
          
     RREETTUURRNNSS
          The  carry  flag  is  cleared  on  success,  or  set on
          failure. The number of characters actually  written  is
          returned in hl.




				    -1-


