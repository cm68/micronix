     PROGRAM
          exit ‐ terminate command file

     USAGE
          exit [ integer ]

     FUNCTION
          Exit  performs  a seek to the end of its standard input
          file.  Thus, if it is invoked inside  a  file  of  com‐
          mands, upon return from the shell will discover an end‐
          of‐file and terminate.

          The optional argument will be returned to the shell  as
          the exit status code.

     SEE ALSO
          if(1), goto(1), sh(1)

















































