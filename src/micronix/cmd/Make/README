the stock micronix make
will read Makefile first, then makefile

this one will read makefile first, then Makefile

and gnu make will read GNUmakefile first.

this is a hack of the cp/m make from MORRIS CODE WORKS to run on micronix
the micronix make is so limited.

