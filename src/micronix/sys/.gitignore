UNUSED
build.h
*.o
unix

#
# The ccc survey writes here: one .o per source that compiled and a
# .log per source that did not, both regenerated by every run.  Four
# of the logs were committed by 1345d2ac, which added the directory
# rather than the files it meant to.
#
cccobj/
